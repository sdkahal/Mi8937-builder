name: Build

on:
  workflow_dispatch:
    inputs:
      kernel_url:
        description: 'Kernel tar.gz download URL'
        required: false
        default: 'https://github.com/sdkahal/Armbian-build/releases/download/v2/linux-7.0.2-msm8937-arm64.tar.gz'
  push:
    branches:
      - '**'
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  decide:
    runs-on: ubuntu-24.04-arm
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    steps:
      - id: check
        name: Check commit message for skip directive
        run: |
          msg="${{ github.event.head_commit.message }}"
          subject="${msg%%$'\n'*}"
          body="${msg#*$'\n'}"
          first_body_line="$(printf "%s" "$body" | awk 'NF{print; exit}')"
          skip="false"
          case "$subject" in
            "skip build:"*) skip="true" ;;
          esac
          case "$first_body_line" in
            "skip build:"*) skip="true" ;;
          esac
          echo "skip=$skip" >> "$GITHUB_OUTPUT"

  build:
    needs: decide
    if: needs.decide.outputs.skip != 'true'
    runs-on: ubuntu-24.04-arm

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: fix 
        run: 
          sudo chmod 755 /home/runner

      - name: Install dependencies
        run: sudo sh -e scripts/install_deps.sh

      - name: Create rootfs
        env:
          KERNEL_URL: ${{ inputs.kernel_url }}
        run: sudo sh -e scripts/debootstrap.sh

      - name: Create images
        run: sudo sh -e scripts/build_images.sh

      - name: Determine artifact name
        id: artifact_name
        run: |
          branch="$GITHUB_REF_NAME"
          case "$branch" in
            debian)
              base="mi8937-debian" ;;
            ubuntu-*)
              base="mi8937-ubuntu" ;;
            *)
              # Fallback: sanitize branch for artifact naming
              sanitized=$(echo "$branch" | tr '[:upper:]' '[:lower:]' | tr -c 'a-z0-9' '-')
              base="mi8937-${sanitized}" ;;
          esac
          echo "base=$base" >> "$GITHUB_OUTPUT"
          echo "zip=${base}.zip" >> "$GITHUB_OUTPUT"
          echo "rawzip=${base}.raw.zip" >> "$GITHUB_OUTPUT"

      - name: Zip artifact
        env:
          ZIP_NAME: ${{ steps.artifact_name.outputs.zip }}
          RAWZIP_NAME: ${{ steps.artifact_name.outputs.rawzip }}
        run: |
          cd files
          zip -r ../"${ZIP_NAME}" .
          echo "Created ${ZIP_NAME}"
          cd ..
          zip -r "${RAWZIP_NAME}" *.raw

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.artifact_name.outputs.base }}
          path: ${{ steps.artifact_name.outputs.rawzip }}

      - name: Create Release for this commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="build-${GITHUB_SHA}"

          branch="$GITHUB_REF_NAME"
          case "$branch" in
            debian)
              human="Debian" ;;
            ubuntu-24.04)
              human="Ubuntu 24.04 (Noble)" ;;
            ubuntu-*)
              ver="${branch#ubuntu-}"
              human="Ubuntu ${ver}" ;;
            *)
              tmp="${branch//-/ }"
              human="$(awk '{for(i=1;i<=NF;i++){ $i=toupper(substr($i,1,1)) substr($i,2)}; print}' <<< "$tmp")" ;;
          esac

          date_str="$(date -u +%Y-%m-%d)"
          title="${human} build: ${date_str}"
          notes="Automated release for commit ${GITHUB_SHA} on ${GITHUB_REF_NAME}"

          asset="${{ steps.artifact_name.outputs.zip }}"

          gh release create "$tag" \
            --target "$GITHUB_SHA" \
            --title "$title" \
            --notes "$notes" \
            --prerelease \
            "$asset"
